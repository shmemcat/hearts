# NextAuth + API (email/password auth)
API_URL=http://localhost:5001
# Browser-side: used when calling the API from the frontend (required with output: "export")
NEXT_PUBLIC_API_URL=http://localhost:5001
NEXTAUTH_URL=http://localhost:3000
JWT_SECRET=at_least_32_characters_random_string

# Mail (dev: MailHog catches mail at http://localhost:8025; prod: set MAIL_SERVER, MAIL_PORT, etc.)
# MAIL_SERVER=mailhog
# MAIL_PORT=1025

# Font Awesome Pro (get your package token from https://fontawesome.com/account)
# Set this in your environment or in .env.local before running npm install in web/
# Never commit the actual token to git.
FONTAWESOME_PACKAGE_TOKEN=E2DB62F4-B68E-473B-9413-88D31830CC15
